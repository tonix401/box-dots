#!/usr/bin/env bash
set -euo pipefail

THEME="$HOME/.config/rofi/emoji.rasi"

command -v rofimoji &>/dev/null || { notify-send "Emoji" "rofimoji is not installed"; exit 1; }

rofimoji --action clipboard --prompt "Emoji " --selector-args "--theme $THEME"